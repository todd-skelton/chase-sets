# Packages

This folder will contain shared libraries used by the apps.

Planned MVP packages:

- `packages/shared`: IDs, time, event envelope, shared utilities.
- `packages/config`: Config-driven schemas (variants, filters) + validation.

See `plan.md` and `artifacts/11-development-standards.md`.
